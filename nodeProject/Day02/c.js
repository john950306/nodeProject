/**
 * http://usejsdoc.org/
 */

var under = require('underscore'); //모듈에있는것은 경로를 생략해도됨






//npm install 패키지명 --save -g(다른프로젝트도가능)
var arr = [4,3,1,6,1,4,7];
var first = arr[0];
//var first = _.first(arr);
console.log(first);


//var last = _.last(arr);
var last = arr[arr.length-1];
console.log(last);












